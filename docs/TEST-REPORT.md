# Отчёт о тестировании

Машина: Linux x86_64 (Debian 13, Mesa llvmpipe, без GPU и звука), Node 20.19, Electron 31.

# Фаза 5 — сборка, CI, автообновление (24.09.2026)

## Автотесты
- `npm test`: **78/78** (новое: `packaging.test.js` — конфиг electron-builder, подпись только по env, репозиторий обновлений, `updateSupport`, классификация ошибок обновления; i18n-тест проверяет динамические ключи `update.*`).
- `node scripts/ci-smoke.js`: 7/7 на linux/x64 (реально); через `--as` смоделированы win32/x64, win32/arm64, darwin/x64, darwin/arm64, linux/arm64 — 7/7 (для linux/arm64 предупреждение: Mojang не публикует natives).
- `actionlint 1.7.12` для `.github/workflows/build.yml` — без замечаний.

## Linux-артефакты (`npm run dist:linux`, собраны локально)
| Файл | Размер |
|---|---|
| `Forja-Launcher-0.3.0-linux-x86_64.AppImage` | ~103 МБ |
| `Forja-Launcher-0.3.0-linux-amd64.deb` | ~72 МБ |
| `Forja-Launcher-0.3.0-linux-x64.tar.gz` | ~97 МБ |
| `latest-linux.yml` | метаданные обновления |

- `dpkg-deb -I`: Depends: libgtk-3-0|libgtk-3-0t64, libnotify4, libnss3, libxss1, libxtst6, xdg-utils, libatspi2.0-0|libatspi2.0-0t64, libuuid1, libsecret-1-0, libgbm1, libdrm2, libxkbcommon0, libasound2|libasound2t64, libegl1, libgl1, ca-certificates; Recommends: libopenal1, x11-xserver-utils, libpulse0; Section: games.
- Содержимое deb: иконки hicolor 16…1024, `.desktop` (`Categories=Game;`, `Icon=forja-launcher`), `app-update.yml` с owner/repo `zhekanisher7-rgb/Forja-launcher`.
- `apt-get -s install ./…deb` на Debian 13 — зависимости разрешаются (реально не устанавливался).
- AppImage запущен (`--appimage-extract-and-run --no-sandbox`, только в командной строке): главное окно — `docs/phase5-appimage.png`; карточка «Обновления лаунчера» обратилась к GitHub — `docs/phase5-updates.png` (снимок сделан до того, как пустой репозиторий стал показываться как «Опубликованных версий пока нет», а не как ошибка).

## Windows из Linux
- Wine не установлен (тяжёлый) → NSIS-установщик собирается только в CI (windows-latest).
- Кросс-сборка zip без rcedit (`signAndEditExecutable: false`) прошла: `Forja-Launcher-0.3.0-win-x64.zip` ~102 МБ, но у exe нет иконки/метаданных — поэтому в CI Windows собирается на windows-latest.

## Не проверено / известные ограничения
- CI ещё ни разу не запускался (репозиторий не запушен); установщики Windows (NSIS) и macOS (dmg) не собирались и не запускались; unit-тесты на Windows/macOS не гонялись.
- Автообновление end-to-end (скачивание и установка новой версии) не проверено — нужен хотя бы один опубликованный релиз и следующий за ним.
- deb реально не устанавливался (только симуляция apt); AppImage требует libfuse2 (иначе `--appimage-extract-and-run`).
- Нет сборки linux-arm64; Windows arm64 — только zip.
- macOS без сертификата: ad-hoc подпись (только при сборке на macOS), автообновление на macOS отключается (`macUnsigned`), Gatekeeper требует «Открыть» через ПКМ или `xattr -dr com.apple.quarantine`.
- electron-builder 24 не находит `electron-builder.config.js` сам — во всех скриптах указан `--config`.
- Запуски игры из фазы 3 по-прежнему не подтверждены вживую из-за нехватки памяти на машине.

# Фаза 3 (24.09.2026)

## Модульные тесты — `npm test`

**72 теста, 72 пройдено** (55 из фазы 2 + 17 новых). Перед тестами `pretest` (`scripts/check-deps.js`)
проверяет, что `node_modules` на месте.

- `test/unit/loaders.test.js` (8): слияние `inheritsFrom` (дочерние библиотеки важнее, аргументы
  склеиваются, `mainClass`/`id` от дочерней версии, jar/assets/Java от родителя); legacy
  `minecraftArguments` (Forge 1.12.2); многоуровневая цепочка и natives-записи; командная строка
  «legacy-основа + современные аргументы дочерней версии»; **подстановка аргументов процессоров Forge**
  (`{DATA}`, `[maven]`, `{MINECRAFT_JAR}`, `{SIDE}`, `{ROOT}`, `/data/...` из установщика); отбор
  процессоров по `sides`, разделение библиотек на скачиваемые и встроенные; NeoForge → версия Minecraft
  и адреса установщиков; сортировка версий загрузчиков, выбор recommended.
- `test/unit/content-mrpack.test.js` (5): **разрешение зависимостей** (обязательные рекурсивно,
  необязательные пропускаются, установленные/дубли не повторяются, отсутствующие сообщаются);
  список контента + **вкл/выкл** (`.jar.disabled`) + удаление с кэшем хэшей; помощники Modrinth
  (facets, выбор версии, User-Agent); **разбор `modrinth.index.json`** (загрузчик из `dependencies`,
  фильтр `env.client`, проверка хэшей и хостов); **overrides** (`client-overrides` важнее, выход за
  папку отклоняется).
- `test/unit/storage.test.js` (4): **подсчёт ссылок при очистке** (всё, что достижимо из профилей,
  остаётся); закреплённая версия загрузчика сохраняет только свою запись, запущенные версии защищены;
  нечитаемая цепочка → библиотеки/Java/ресурсы не трогаются; подсчёт места.
- `test/unit/i18n.test.js` теперь проверяет и `mods.js` (≈130 новых ключей, ru = en).

## Сбой машины во время проверок (≈22:15)

Во время запуска Forge 1.20.1 из интерфейса (Electron + игра на программном OpenGL) машина
перестала отвечать и была перезапущена. После перезапуска:

- **Код не потерян.** Все правки фазы 3 оказались на диске (сохранены коммитом `80b55e8`).
- `npm test` давал 54 теста / 5 падений: причина — **исчезла папка `node_modules`** (в git не входит).
  Пять «файлов»-падений — это модули, которые подключают `adm-zip` (`Cannot find module 'adm-zip'`).
  Исправлено: `npm ci` → 72/72. Добавлен `pretest` с понятным сообщением
  «Missing dependencies… Run npm ci».
- Пропали: журналы и скриншоты в `/tmp`, **профиль «Forge 1.20.1»**, созданный перед сбоем
  (`profiles.json` откатился к более раннему состоянию). Загруженные файлы в `/workspace/mc-data`
  и `loaders.json` сохранились. Профиль Forge 1.20.1 создан из интерфейса заново.
- Свободной памяти после перезапуска ≈3.8 ГБ из 16 ГБ даже без Electron и игр: ≈10 ГБ занято вне
  процессов бокса (в системе есть `virtio_balloon`, т. е. память удерживает хост). Правило для
  повторных проверок — **запускать игру, только если доступно ≥ 5 ГБ** — поэтому после перезапуска
  игры **не запускались** (см. таблицу).

## Проверки загрузчиков

| Загрузчик | Установка | Запуск | Доказательство |
|---|---|---|---|
| Fabric 1.20.1 (0.19.5, «последняя стабильная») | meta v2 | **главное меню** из интерфейса (до сбоя): `Loading Minecraft 1.20.1 with Fabric Loader 0.19.5`, `Loading 60 mods: fabric-api 0.92.12, modmenu 7.2.2, sodium 0.5.13`; экран «Mods» (Mod Menu), надпись «Fabric (60 Mods)» | `docs/phase3-fabric-game.png`, `instances/fabric-1-20-1-a8b494/logs/latest.log` |
| Forge 1.20.1 47.4.10 (recommended) | официальный установщик, процессоры выполнены, SHA1 выходов совпали | без интерфейса (до сбоя): `Forge mod loading, version 47.4.10`, созданы атласы текстур; из интерфейса дошёл до экрана загрузки «Forge loading 47.4.10», после чего машина зависла. **Главное меню не подтверждено** | журнал/скриншот были в `/tmp` и потеряны |
| Forge 1.12.2 14.23.5.2859 (legacy) | установщик spec 0, universal jar из `maven/` | без интерфейса (до сбоя): `Forge Mod Loader has identified 4 mods to load`, LWJGL 2.9.4, Java 8. Главное меню не подтверждено | журнал потерян |
| Quilt 1.20.1 (0.30.1) | meta v3 | без интерфейса (до сбоя): `Loading Minecraft 1.20.1 with Quilt Loader 0.30.1`, `Setting user`. Главное меню не подтверждено | журнал потерян |
| NeoForge 21.1.251 (1.21.1) | установщик + процессоры | без интерфейса (до сбоя): `NeoForge mod loading, version 21.1.251`, LWJGL 3.3.3. Главное меню не подтверждено | журнал потерян |

После перезапуска все пять загрузчиков проверены **без запуска игры**
(`node scripts/verify-profiles.js` — тот же код, что «Проверить и восстановить»): версия-цепочка
читается, все библиотеки/ресурсы/Java совпадают по SHA1 (см. «Очистка»).

## Modrinth и модпаки (интерфейс Electron)

| Проверка | Результат | Доказательство |
|---|---|---|
| Профиль с загрузчиком из редактора | «Fabric 1.20.1»: загрузчик Fabric, версия по умолчанию — последняя стабильная (0.19.5); для Forge предвыбрана recommended 47.4.10 и показана подсказка о поддержке проекта | `docs/phase3-edit-loader.png` |
| Поиск | «sodium» с фильтром 1.20.1 + Fabric, категории, сортировка, страницы | `docs/phase3-mods-search.png` |
| Страница проекта | описание (безопасный Markdown), значок, загрузки, список версий | `docs/phase3-project.png` |
| Установка + зависимости | Sodium; Mod Menu (у 1.20.1-версии в Modrinth нет зависимостей — ничего лишнего не поставлено); **Sodium Extra → автоматически поставлен Fabric API** (Sodium уже был); SHA-1 + SHA-512 проверены | `ls mods/` |
| Выключение | переключатель → `sodium-extra-…jar.disabled`, строка приглушена | — |
| Обновления | подложен официальный старый `modmenu-7.2.1.jar` → опознан по хэшу, бейдж «→ 7.2.2», «Обновить все (1)» → `modmenu-7.2.2.jar`, старый удалён | `docs/phase3-installed-mods.png` |
| Удаление | окно подтверждения → файл удалён | — |
| Модпак из поиска | «Fabric Optimized», выбрана версия 1.20.1+1.2 → новый профиль «Optimized Fabric 1.20.1», загрузчик **Fabric 0.14.22 взят из `dependencies`**, 20 файлов, **sha512 всех 20 совпадает** с индексом, в профиле записан источник (`modpack`) | `docs/phase3-modpack-installed.png` |
| Запуск модпака | **не выполнялся** (после перезапуска доступно < 5 ГБ памяти); файлы профиля и Fabric 0.14.22 проверены `verify-profiles.js` | — |

## Очистка диска

1. До очистки созданы профили Quilt 1.20.1, Forge 1.12.2, NeoForge 1.21.1 (чтобы их файлы считались используемыми).
2. `verify-profiles.js` до очистки: 7 профилей «OK»; «Default» (1.16.5 — в фазе 2 его установка была
   отменена) докачал 231 файл; модпак докачал 10 библиотек нового Fabric 0.14.22.
3. «Найти неиспользуемое»: версии **1.5.2, 1.6.4, 26.3**, 130 библиотек, Java `java-runtime-epsilon`,
   3 индекса и 5068 объектов ресурсов — **707 МБ**. (Без трёх новых профилей в план попадали и
   1.12.2-forge, neoforge, quilt — 1.26 ГБ; скриншот первого варианта заменён.)
   → окно подтверждения → «Удалить». Итог: **2.80 ГБ → 2.11 ГБ**.
4. `verify-profiles.js` после очистки: **все 9 профилей «OK», перекачано 0 файлов**
   (Fabric ×2, Quilt, Forge ×2, NeoForge, 3 vanilla). Файлы-выходы процессоров из `loaders.json` на месте.
   Найдено и исправлено: в `extraFiles` Forge/NeoForge записывался `client-…-unpacked.jar`
   (данные `MC_UNPACKED` нужны только серверным процессорам и на клиенте не создаются) — теперь
   записываются только реально существующие файлы.
5. Повторный запуск игры после очистки **не выполнялся** (ограничение памяти, см. выше).

Скриншоты: `docs/phase3-storage.png` (план), `docs/phase3-storage-after.png` (после очистки).

## Известные проблемы фазы 3 / что не проверено

1. **Не подтверждено главное меню** для Forge 1.20.1 (из интерфейса), Forge 1.12.2, Quilt, NeoForge,
   модпака и запуска после очистки — из-за нехватки памяти на тестовой машине после сбоя.
   Нет `docs/phase3-forge-game.png` и `docs/phase3-modpack.png` с игрой.
2. Установщик Forge в `_comment_` просит не автоматизировать загрузку в обход его рекламной страницы;
   лаунчер берёт установщик с официального maven и показывает ссылку на поддержку проекта.
3. У Forge `logging: {}` — XML-конфиг логов vanilla для Forge не подключается (Forge настраивает логи сам).
4. «Последняя стабильная» закрепляется в профиле при первом запуске (дальше обновление — вручную в редакторе).
5. Очистка консервативна: при любой нечитаемой версии библиотеки/Java/ресурсы не удаляются; папки
   профилей (миры, моды) и `adoptium-*` не удаляются никогда.
6. Шейдеры только копируются в `shaderpacks/`; без шейдерного мода (Iris/OptiFine) они не работают.
7. В User-Agent Modrinth стоит адрес-заглушка `forja-launcher@example.invalid` — перед релизом указать реальный контакт.
8. CurseForge не подключён (API требует ключ, который нельзя хранить в клиенте).
9. Windows/macOS вживую не проверялись; звука на тестовой машине нет.
10. Закрытие окна игры через `xdotool windowclose` на тестовой машине даёт `X Error BadDrawable` и
    код 1 (окно сбоя) — артефакт среды; закрывать игру нужно из её меню.
11. Экран Mods в игре не показывает Fabric API (Mod Menu
    по умолчанию скрывает библиотеки) — это поведение мода, в журнале Fabric API загружен.

---

# Фаза 2 (24.09.2026)

## Модульные тесты — `npm test`

**55 тестов, 55 пройдено** (37 из фазы 1 + 18 новых):

- `test/unit/profiles.test.js` — миграция `instances/default` + настроек v1 в профиль «Default»
  (версия, JVM-аргументы, разрешение), создание (обязательна версия, уникальный id с транслитерацией
  кириллицы, папка `instances/<id>`), изменение, `touch` (lastPlayed), дублирование с копированием папки и без,
  удаление с папкой и без, запрет удалить последний профиль, сохранение/перечитывание `profiles.json`
  со `schemaVersion`, откат на `.bak` при повреждённом файле, нормализация некорректных полей.
- `test/unit/settings-natives.test.js` — сохранение и нормализация настроек, миграция v1 → v2;
  уникальная папка natives на каждый запуск, удаление после выхода, уборка брошенных папок
  (владелец мёртв) без удаления чужих живых; разбор JVM-аргументов с кавычками; классификация ошибок
  (сеть, ENOSPC, EACCES, Java и др.).
- `test/unit/games.test.js` — две игры одновременно (фейковый launchFn), повторный запуск того же профиля → `ALREADY_RUNNING`,
  «Закрыть игру» не считается сбоем, сбой (код ≠ 0) → событие с последними 50 строками и путём к crash-report,
  отмена установки, состояние ошибки.
- `test/unit/i18n.test.js` — одинаковые ключи и плейсхолдеры в `ru.json` и `en.json`, все ключи из кода существуют,
  у каждого кода ошибки есть сообщение.

## Интеграционный тест — `FORJA_IT_DIR=/workspace/mc-data npm run test:integration`

2 теста, пройдено: 1.20.1 (3652 файла) и 1.8.9 (769 файлов), ошибок SHA1: 0. Тест обновлён под natives на запуск:
проверяет, что временная папка создана, содержит `.so` и удалена после очистки.

## Интерфейс Electron (рабочий стол бокса, `FORJA_DATA_DIR=/workspace/mc-data`)

| Проверка | Результат | Доказательство |
|---|---|---|
| Миграция данных фазы 1 | `settings.json` → v2, в `profiles.json` появился профиль «Default» (1.16.5, `instances/default`) | `profiles.json` |
| Создание профилей из UI | «Выживание 1.20» (1.20.1, значок-кристалл, синий, 3 ГБ, 1280×720, `-XX:+UseG1GC -Dforja.profile=survival`) и «PvP 1.8.9» (буква P, красный) | `docs/phase2-edit-profile.png` |
| Два запуска одновременно | обе игры дошли до главного меню; `--gameDir` = `instances/vyzhivanie-1-20-263b3d` и `instances/pvp-1-8-9-aa8fc2`; 1.20.1 открылась в 1280×720; бейджи «запущено», «Запущено игр: 2» | `docs/phase2-profiles.png`, `docs/phase2-both-games.png`, `docs/phase2-game-1.20.1.png` |
| Natives на запуск | при двух играх — две папки в `tmp/natives/`, после выхода обеих — 0 | `ls tmp/natives` |
| «Закрыть игру» | обе игры закрыты, окна сбоя нет, статус «Игра закрыта из лаунчера» | — |
| Дублирование + изменение | копия PvP → «Crash test 1.8.9», своя папка `instances/pvp-1-8-9-kopiya-2a561e`, JVM `-XX:+ThisOptionDoesNotExist` | тост «Профиль сохранён» |
| Имитация сбоя | окно «Игра завершилась с ошибкой»: код 1, `Unrecognized VM option 'ThisOptionDoesNotExist'`, `Could not create the Java Virtual Machine`; папка natives удалена | `docs/phase2-crash.png` |
| «Открыть папку crash-reports» | папка создана и открыта системным обработчиком | — |
| Проверить и восстановить | чистая версия: «восстановлено файлов: 0»; после порчи `versions/1.8.9/1.8.9.jar` — «восстановлено файлов: 1», SHA1 снова `3870888a…` | `docs/phase2-repair.png` |
| Настройки | автосохранение; переключение ru → en сразу меняет весь интерфейс, обратно ru | `docs/phase2-settings.png`, `docs/phase2-settings-en.png` |
| Журнал | общий журнал с префиксом профиля, фильтр по профилю | `docs/phase2-log.png` |
| Удаление профиля | окно подтверждения, флажок «удалить папку» → профиль и `instances/pvp-1-8-9-kopiya-2a561e` удалены, выбран «Default» | тост «Профиль удалён» |
| Значок-буква | буква берётся из названия («Hardcore» → H), пока пользователь не ввёл свою | — |

## Известные проблемы фазы 2 / что не проверено

1. **Windows/macOS** по-прежнему вживую не проверялись (временные natives сделаны именно ради Windows).
2. Режимы «Скрыть лаунчер» / «Закрыть лаунчер» при запуске игры реализованы, но в интерфейсе вживую не проверялись.
   В режиме «закрыть» игра запускается отсоединённым процессом, временную папку natives убирает уборка при следующем старте лаунчера.
3. Уведомления «нет сети» / «мало места» проверены модульными тестами (классификация, проверка места до загрузки),
   но в UI не имитировались (сеть на тестовой машине не отключали).
4. Имена пользователей / пути данных с не-ASCII символами могут мешать Java 8 + LWJGL 2 на Windows (не проверялось).
5. Окно редактора профиля выше маленького окна (≈730 px) — тело окна прокручивается; кнопки «Сохранить/Отмена» закреплены.
6. Кириллицу в поле ввода автоматический тест набирал через `setxkbmap ru` + `xdotool` — это ограничение средства автоматизации, не лаунчера.
7. `build/icon.icns` собран скриптом из PNG (icp4…ic10) без `iconutil`; на macOS при релизе стоит перепроверить (фаза 5).
8. ~~Удаление неиспользуемых версий/библиотек пока нет~~ — сделано в фазе 3.

---

# Фаза 1

## Модульные тесты — `npm test`

37 тестов, 37 пройдено: rules (os/arch/version/features, «последнее правило решает»),
maven-пути и разрешение библиотек (artifact, без downloads, legacy classifiers с `${arch}`,
фильтр natives по архитектуре), аргументы (modern/legacy, порядок, `-Xms/-Xmx`, разделители
classpath для Windows/unix, `-XstartOnFirstThread` на macOS, custom resolution, log4j, `${game_assets}`),
подстановка плейсхолдеров, offline UUID (сверено с `java.util.UUID.nameUUIDFromBytes` на Java 17:
Notch → `b50ad385-829d-3141-a216-7e7d7539ba7f`), пути данных по ОС, маппинг платформ Java runtime,
фильтр версий, `inheritsFrom`, загрузчик (параллельность, SHA1, пропуск, повтор после 500,
повторная загрузка испорченного файла, 404, отмена, докачка через Range), парсер XML-логов.

## Интеграционный тест — `npm run test:integration`

Чистая временная папка, только core-модули (без Electron), затем независимая перепроверка SHA1 каждого файла.

| Версия | Проверено файлов | Объём | Библиотеки+клиент | Ресурсы | Java | Время установки |
|---|---|---|---|---|---|---|
| 1.20.1 | 3652, ошибок SHA1: 0 | 697.8 МБ | 54 файла, 77.3 МБ | 3575 файлов, 620.2 МБ | 17.0.15 (Mojang), 133 файла, 95.2 МБ | 35.5 с + Java 3.7 с |
| 1.8.9 | 769, ошибок SHA1: 0 | 131.4 МБ | 35 файлов, 21.8 МБ | 722 файла (187 новых, 8.3 МБ; остальные общие с 1.20.1) | 8u202 (Mojang), 300 файлов, 224.2 МБ | 5.2 с + Java 4.5 с |

Итого тест ~51 с. Также проверено: natives распакованы (`.so`), все элементы classpath существуют,
в аргументах нет нераскрытых `${…}`, мажорная версия Java совпадает с требуемой.

## Реальный запуск игры

`DISPLAY=:3 LIBGL_ALWAYS_SOFTWARE=1`, режим «Офлайн (тест)». Ни в одном запуске нет
`ClassNotFoundException`, `NoClassDefFoundError` или `UnsatisfiedLinkError`.

| Версия | Как запускали | Результат |
|---|---|---|
| 1.20.1 | `scripts/headless-launch.js` | `Setting user`, `Backend library: LWJGL version 3.3.1`, атласы текстур, главное меню — `screenshot-game.png` |
| 1.8.9 | headless (legacy `minecraftArguments` + natives classifiers, Java 8) | `LWJGL Version: 2.9.4`, главное меню — `screenshot-game-1.8.9.png` |
| 1.12.2 | **через интерфейс Electron** (установка с нуля + запуск) | главное меню — `screenshot-ui-progress.png`, `screenshot-ui-game-running.png`, `screenshot-ui.png` |
| 1.16.5 | через интерфейс: установка, затем «Отмена» | загрузка остановлена, статус «Отменено» |
| 1.6.4 | headless (virtual/legacy assets) | `Setting user`, LWJGL 2.9.0 |
| 1.5.2 | headless (`map_to_resources`, launchwrapper), отдельная игровая папка | главное меню — `screenshot-game-1.5.2.png` |
| 26.3 (последний релиз) | headless (Java 25, SDL3) | OpenGL 4.5 (Mesa), главное меню — `screenshot-game-26.3.png` (понадобился системный `libegl1`, см. ниже) |

Ошибки в логах, связанные со средой, а не с лаунчером: нет звуковой карты (ALSA/OpenAL →
игра переходит в режим без звука); `401 Failed to verify authentication` / `Failed to fetch user properties` /
Realms `Failed to parse into SignedJWT` — ожидаемо для офлайн-режима без токена.

## Известные проблемы фазы 1 (статус на сейчас)

1. **Windows и macOS не тестировались вживую.** Код учитывает: `javaw.exe`, `jre.bundle/Contents/Home/bin/java`,
   `-XstartOnFirstThread`, разделитель `;`, `%APPDATA%`, симлинки Java runtime (на Windows пропускаются — в Windows-runtime Mojang их нет).
2. ~~Общая игровая папка `instances/default` (падение старых версий на `options.txt` от новых)~~ — **исправлено в фазе 2**: у каждого профиля своя папка.
3. **Linux:** для новых версий на SDL3 (26.x) нужен системный `libEGL` (`libegl1`), иначе «Couldn't find matching GLX visual».
   Надо описать в требованиях / deb-зависимостях (фаза 5).
4. **Linux arm64 / прочие архитектуры:** Mojang runtime нет → Adoptium (код есть, вживую не проверялся);
   у старых версий нет arm64-natives в JSON.
5. Правила `os.version` сверяются с `os.release()` (на macOS это версия ядра Darwin, а не macOS). Затрагивает только
   очень старые правила (`^10\.5\.\d$`).
6. ~~Общая папка natives (блокировка файлов на Windows)~~ — **исправлено в фазе 2**: временная папка на каждый запуск.
7. При каждом запуске ресурсы перепроверяются по SHA1 (~1–3 с на 700 МБ с тёплым кэшем ФС); Java runtime после
   первой проверки сверяется только по размеру.
8. ~~Нет иконки~~ — иконки есть в `build/` (фаза 2); автообновление и подпись — фаза 5.
9. `--no-sandbox` использовался только для запуска Electron на тестовой машине; в код и сборку он не добавлен.
